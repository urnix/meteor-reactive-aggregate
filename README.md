# meteor-reactive-observable-aggregations

Reactively publish aggregations **with the ability to observe the items**.

## Installation

``` sh
$ meteor add artemi:reactive-observable-aggregations
```
